%noscript
	For full functionality of this site it is necessary to enable JavaScript.
	%br
	Here are the
	%a{:href => "http://enable-javascript.com/"}
		instructions on how to enable JavaScript in your web browser
- cache "header_#{@abstract_model.pretty_name.downcase unless @page_type == "dashboard"}s" do
	#hiddenForNavHighlight{:style => "display:none;"}
		%input{:type => "text", :value => "#{@abstract_model.pretty_name.gsub(/\s/, "_").gsub(/[^\w-]/, '').downcase unless @page_type == "dashboard"}s", :id => "hiddenForNavHighlightInput"}
- cache("header_toolbar_for_#{current_user.id}") do
	#header.my_header
		%h1.title
			%a{:href => '/admin'}
				Hospitium

		#user-navigation
			%ul.wat-cf
				%li
					%a{:href => "https://hospitium.tenderapp.com/kb", :target => "blank"}
						%img{:alt => "Help", :src => "https://d4uktpxr9m70.cloudfront.net/stylesheets/rails_admin/theme/activo/images/session/help3.png", :title => "Help"}
				%li= link_to header_icon(:config, t('admin.dashboard.name')), rails_admin_dashboard_path
				- if (root_path rescue nil)
					%li= link_to header_icon(:home, t('home.name').capitalize), root_path
				- if (current_user rescue nil)
					%li= link_to header_icon(:account, current_user.email), (rails_admin_show_path(:user, current_user) rescue '#')
				- if (current_user rescue nil)
					%li= link_to header_icon(:logout, t("admin.credentials.log_out")), (destroy_session_path(current_user) rescue '#'), :method => :delete

